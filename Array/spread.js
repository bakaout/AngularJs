const num =  [1, 2, 3, 4, 5]

console.log(Math.max(...num))//utilizando os tres pontinhos, é possivel emtrar direto na lista para identificar os valores(nesse caso foi para identificar o maior valor do array)